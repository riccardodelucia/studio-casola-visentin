---
const { title } = Astro.props;
---

<article class="card">
  <div class="card-content">
    <h2>{title}</h2>
    <slot />
  </div>
</article>

<style>
  .card {
    border-radius: var(--border-radius);
    background-color: rgba(13, 55, 94, 0.73);
    padding-bottom: 2rem;
    overflow: hidden;
  }

  .card-content {
    font-size: var(--font-size-2);
    padding-block: 0.5em;
    padding-inline: 0.6em;
    font-size: var(--font-size-1);
  }

  .card-content h2 {
    color: var(--text-color-white);
    font-size: var(--font-size-3);
    margin-top: 1.2rem;
    margin-bottom: 1rem;
  }

  .card > img {
    width: 100%;
  }
</style>
